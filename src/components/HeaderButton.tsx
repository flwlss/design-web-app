import React, { useState } from "react";

const HeaderButton = () => {

  return (
    <>
      <button
        onClick={() => {}}
        className="header__button">
        Заказать проект
      </button>
    </>
  )
}

export default HeaderButton